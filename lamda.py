# anonymous functions that are without a name 
# can  have any number of arguments but only one expression which is evaluated and returned
# they are syntatically restricted to only one function
# lambda_cube=lambda y:y*y*y
# print(lambda_cube(100))
tables=[lamda x=x:x*10 for x in range (1,11)]
tables in tables:
print(tables())